<UserControl x:Class="Fcog.Controls.Wpf.Recognition.DataSetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fcog.Controls.Wpf.Recognition"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GroupBox Name="GroupBoxstatistics" Header="Statistics" Grid.Row="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Row="0">
                    <Label>Total: </Label>
                    <Label Name="LabelTotalData"></Label>
                </StackPanel>

                <ListBox Name="ListBoxStatistics" Grid.Row="1" />
            </Grid>
        </GroupBox>

        <GroupBox Name="GroupBoxData" Header="Data" Grid.Row="1">
            <ListBox Name="ListBoxData"></ListBox>
        </GroupBox>
    </Grid>
</UserControl>
